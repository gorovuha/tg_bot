# Required — token from @BotFather
TELEGRAM_BOT_TOKEN=your_token_here

# Classifier backend: mock (default, keyword heuristics for demo) | retrieval (phase 2, EUvsDisinfo index)
CLASSIFIER_BACKEND=mock

# SQLite path (default: ./data/bot.db)
# BOT_DB_PATH=/data/bot.db
